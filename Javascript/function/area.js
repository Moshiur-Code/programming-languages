// The program can calculate area of a rectangle

function findArea(height, width) {
    let result = height * width;
    return result;
}

let height = 5, width = 6; 
let area = findArea(height, width);
console.log(area);