// Here we will show the use of "&&", "||" shorthand

// use of ||
var Name = ''
var fullname = Name || 'Moshiur'
console.log(fullname)

// use of &&
var value = true
value && console.log("Everything is fine")

var value = false
value && console.log("Everything is fine")